export const CHUNK_SIZE_BYTES = 20 * 1024 * 1024;
// DEAD EXPORT — verify before removing
export const SUPPORTED_LANGUAGES = ['EN', 'FR'] as const;
export const SUPPORTED_AUDIO_FORMATS = ['mp3', 'mp4', 'wav', 'm4a', 'webm', 'aac'] as const;
export const MAX_UPLOAD_SIZE_BYTES = 500 * 1024 * 1024;
export const WARN_UPLOAD_SIZE_BYTES = 500 * 1024 * 1024;
