---
phase: 01-audio-recording
plan: 03
type: execute
wave: 3
depends_on: [01-01-PLAN.md, 01-02-PLAN.md]
files_modified:
  - src/screens/MeetingScreen.tsx
  - src/store/appStore.ts
autonomous: true
requirements: [AUDIO-01, AUDIO-05, AUDIO-06]
must_haves:
  truths:
    - "User can tap a recording button to start recording audio"
    - "Recording state is clearly displayed (idle, recording, processing)"
    - "User can tap End Meeting to stop recording and transition to processing"
    - "User can cancel a recording and discard all audio chunks"
    - "Chunk progress is displayed dynamically during recording (Chunk 2/5...)"
    - "User can tap Upload Audio to select and chunk an existing file"
  artifacts:
    - path: "src/screens/MeetingScreen.tsx"
      provides: "Full recording/upload UI with state display and cancel"
      min_lines: 80
    - path: "src/store/appStore.ts"
      provides: "Store actions consumed by UI (minor additions if any)"
      contains: "handleRecordingChunk|handleUploadChunk"
  key_links:
    - from: "src/screens/MeetingScreen.tsx"
      to: "src/services/recorder.ts"
      via: "useRecordingController hook import and usage"
      pattern: "useRecordingController"
    - from: "src/screens/MeetingScreen.tsx"
      to: "src/services/uploader.ts"
      via: "useUploadController hook import and usage"
      pattern: "useUploadController"
    - from: "src/screens/MeetingScreen.tsx"
      to: "src/store/appStore.ts"
      via: "appState, chunkCount, currentChunkIndex, processingStep selectors"
      pattern: "useAppStore"
---

<objective>
Implement the full recording UI with state display, chunk progress, cancel, and upload integration.
Purpose: Replace the placeholder MeetingScreen with a working UI that connects to the recording and upload services from Plans 01 and 02, giving users clear visual feedback at every step.
Output: src/screens/MeetingScreen.tsx with complete state-driven UI
</objective>

<execution_context>
@C:/Users/hp/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/hp/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-audio-recording/01-CONTEXT.md
@src/config.ts
@src/store/appStore.ts
@src/screens/MeetingScreen.tsx
@src/components/PrimaryButton.tsx
@src/components/ScreenShell.tsx

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/store/appStore.ts:
```typescript
export type AppState = 'FORM' | 'READY' | 'RECORDING' | 'PROCESSING' | 'RESULTS';

type State = {
  appState: AppState;
  audioChunks: string[];
  chunkCount: number;
  currentChunkIndex: number;
  processingStep: string;
  meetingTitle: string;
  meetingDate: string;
  setAppState: (appState: AppState) => void;
  setMeeting: (meeting: { id: number | null; title: string; date: string }) => void;
  addAudioChunk: (uri: string) => void;
  setChunkState: (chunkCount: number, currentChunkIndex: number) => void;
  setProcessingStep: (processingStep: string) => void;
  resetMeeting: () => void;
  handleRecordingChunk: (uri: string, index: number) => void;
  handleUploadChunk: (uri: string, chunkIndex: number, totalChunks: number) => void;
};
```

From src/components/PrimaryButton.tsx:
```typescript
type Props = {
  label: string;
  onPress: () => void;
  variant?: 'primary' | 'danger' | 'ghost';
};
export function PrimaryButton({ label, onPress, variant = 'primary' }: Props): JSX.Element;
```

From src/components/ScreenShell.tsx:
```typescript
type Props = { children: React.ReactNode };
export function ScreenShell({ children }: Props): JSX.Element;
```

From src/services/recorder.ts (Plan 01 creates this):
```typescript
export function useRecordingController(): {
  start: () => Promise<void>;
  stop: () => Promise<void>;
  cancel: () => Promise<void>;
};
```

From src/services/uploader.ts (Plan 02 creates this):
```typescript
export function useUploadController(): {
  upload: () => Promise<void>;
};
```
</interfaces>
</context>

<tasks>
<task type="auto">
<name>Task 1: Rewrite MeetingScreen with state-driven recording and upload UI</name>
<files>src/screens/MeetingScreen.tsx</files>
<read_first>
src/screens/MeetingScreen.tsx
src/store/appStore.ts
src/components/PrimaryButton.tsx
src/components/ScreenShell.tsx
src/services/recorder.ts
src/services/uploader.ts
</read_first>
<action>
Rewrite `src/screens/MeetingScreen.tsx` to replace all placeholder UI with a fully functional recording and upload interface. The file must implement the following state-driven UI:

**FORM state (existing — keep and refine):**
- Meeting title TextInput with placeholder "Meeting title" (keep current styling)
- Date label (keep current dateLabel useMemo)
- "Create Meeting" PrimaryButton that validates non-empty title, calls setMeeting, transitions to READY

**READY state:**
- Large circular recording button: Pressable with width/height 160, borderRadius 80, backgroundColor '#f59e0b', displays "MIC" text centered in amber/bold style — pressing this calls `recordingController.start()` which transitions to RECORDING state (via useRecordingController hook)
- "Tap to start recording" label below the button (white, fontSize 16)
- "Upload Audio" PrimaryButton with variant="ghost" below the label — pressing this calls `uploadController.upload()` which opens the file picker and transitions to PROCESSING state (via useUploadController hook)
- "Cancel" PrimaryButton with variant="ghost" — pressing this calls resetMeeting() and returns to FORM

**RECORDING state:**
- "Recording..." heading text (white, fontSize 24, fontWeight 700)
- Red recording indicator: a View with width/height 24, borderRadius 12, backgroundColor '#f87171' (red dot pulsing is at the agent's discretion — a static red dot is sufficient)
- Dynamic chunk progress: `Chunk {currentChunkIndex + 1} / {chunkCount}` text in red (#f87171) — read currentChunkIndex and chunkCount from the store
- "End Meeting" PrimaryButton with variant="danger" — pressing this calls `recordingController.stop()` then `setAppState('PROCESSING')`
- "Cancel Recording" PrimaryButton with variant="ghost" — pressing this calls `recordingController.cancel()` which resets state and discards chunks

**PROCESSING state:**
- "Processing..." heading text (white, fontSize 22, fontWeight 700)
- Processing step text from store: `processingStep` value displayed in gray (#94a3b8)
- NOTE: The "Show Results" button must be REMOVED from this state. In this phase, PROCESSING only means chunking is finishing — there is no transcription yet (Phase 2). The app stays in PROCESSING state showing chunking progress. The transition to RESULTS will be handled by Phase 2 when transcription completes.
- "New Meeting" PrimaryButton — pressing this calls resetMeeting() to go back to FORM and discard all data

**RESULTS state (keep minimal for now):**
- "Results" heading (white, fontSize 24, fontWeight 700)
- Display number of audio chunks ready: "{chunkCount} audio chunk(s) ready for transcription" in gray
- "New Meeting" PrimaryButton — pressing this calls resetMeeting()

**Hooks and imports:**
- Import `useRecordingController` from `../services/recorder`
- Import `useUploadController` from `../services/uploader`
- Use `useAppStore` selectors for: appState, chunkCount, currentChunkIndex, processingStep, setAppState, setMeeting, resetMeeting
- Keep the existing `useState` for title input
- Remove the `showUpload` state variable and the placeholder Modal — the upload is now handled by useUploadController.upload() which opens the system file picker directly

**Error handling in UI:**
- Wrap recordingController.start() in try/catch — on error, show Alert.alert('Recording Error', error.message) and stay in READY state
- Wrap uploadController.upload() in try/catch — on error, show Alert.alert('Upload Error', error.message) and stay in READY state

**Styling:** Use the existing inline style patterns from the current file. Dark theme colors: background #0b1220 (via ScreenShell), card background #111827, text white, secondary text #94a3b8, accent #f59e0b (amber), danger #b91c1c, red text #f87171.
</action>
<verify>
<automated>npx tsc --noEmit 2>&1 | Select-String "MeetingScreen" ; if (-not $?) { echo "No type errors in MeetingScreen.tsx" }</automated>
</verify>
<done>
- MeetingScreen.tsx imports useRecordingController and useUploadController
- FORM state has title input and Create Meeting button
- READY state has MIC button, Upload Audio button, and Cancel button
- RECORDING state shows dynamic chunk progress and has End Meeting + Cancel Recording buttons
- PROCESSING state shows processingStep text, no Show Results button
- RESULTS state shows chunk count and New Meeting button
- Placeholder upload Modal is removed
- Error handling wraps start() and upload() with Alert
- TypeScript compilation passes
</done>
</task>

<task type="auto">
<name>Task 2: Ensure store actions align with UI consumption and add missing handlers</name>
<files>src/store/appStore.ts</files>
<read_first>
src/store/appStore.ts
src/services/recorder.ts
src/services/uploader.ts
</read_first>
<action>
1. Read the current `src/store/appStore.ts` and verify the following actions exist and are correctly typed. If any are missing from Plans 01 or 02, add them:

- `handleRecordingChunk: (uri: string, index: number) => void` — Must call addAudioChunk(uri), setChunkState with the current chunk count and index, setProcessingStep('Recording chunk ' + (index + 1) + '...')
- `handleUploadChunk: (uri: string, chunkIndex: number, totalChunks: number) => void` — Must call addAudioChunk(uri), setChunkState(totalChunks, chunkIndex), setProcessingStep('Chunking audio ' + (chunkIndex + 1) + '/' + totalChunks + '...')

2. If `handleRecordingChunk` or `handleUploadChunk` are already present (added by Plan 01 or 02), do NOT duplicate them — just verify they match the signatures above.

3. Ensure `resetMeeting()` also clears `audioChunks: []`, `chunkCount: 0`, `currentChunkIndex: 0`, and `processingStep: ''` — verify these are in the `initial` object. If any are missing from the initial reset, add them.

4. Verify the State type definition includes all actions that the UI will call. No TypeScript errors should remain.
</action>
<verify>
<automated>npx tsc --noEmit 2>&1 | Select-String "appStore" ; if (-not $?) { echo "No type errors in appStore.ts" }</automated>
</verify>
<done>
- handleRecordingChunk action exists and calls addAudioChunk + setChunkState + setProcessingStep
- handleUploadChunk action exists and calls addAudioChunk + setChunkState + setProcessingStep
- resetMeeting clears audioChunks, chunkCount, currentChunkIndex, processingStep
- State type includes all actions consumed by MeetingScreen.tsx
- TypeScript compilation passes with no errors
</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User → UI | Button presses trigger state transitions and service calls |
| UI → Store | Zustand store mutations from user interactions |
| UI → Services | Recording and upload controller calls |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | Elevation | MeetingScreen.tsx | mitigate | Wrap recordingController.start() and uploadController.upload() in try/catch, show Alert on error instead of crashing |
| T-01-08 | Denial of Service | Cancel Recording | mitigate | Cancel button calls recorderController.cancel() which cleans temp files and resets state — prevents orphaned temp files filling storage |
| T-01-09 | Repudiation | PROCESSING state | accept | No audit trail needed for local recording — no sensitive action to repudiate |
</threat_model>

<verification>
- MeetingScreen.tsx imports useRecordingController and useUploadController hooks
- Each AppState renders a distinct UI section
- RECORDING state shows dynamic chunk count from store
- Cancel recording button calls cancel and resets to FORM/READY
- Upload Audio button opens file picker via useUploadController
- No placeholder Modal or text remains
- TypeScript compilation passes
</verification>

<success_criteria>
- User can create a meeting (FORM → READY)
- User can start recording (READY → RECORDING) with MIC button
- User sees "Recording..." with dynamic chunk progress
- User can end recording (RECORDING → PROCESSING) with End Meeting button
- User can cancel recording, discarding all chunks and returning to READY
- User can upload audio file, transitioning to PROCESSING
- PROCESSING state shows chunking progress text, no Show Results button
- No placeholder UI remains in MeetingScreen
</success_criteria>

<output>
After completion, create `.planning/phases/01-audio-recording/01-03-SUMMARY.md`
</output>
